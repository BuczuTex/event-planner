import { CustomNgxDateTimeAdapter } from './custom-ngx-date-time-adapter';

describe('CustomNgxDateTimeAdapter', () => {
  it('should create an instance', () => {
    expect(new CustomNgxDateTimeAdapter()).toBeTruthy();
  });
});
